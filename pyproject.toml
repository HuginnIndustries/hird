[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hird"
version = "0.0.1"
description = "Multi-agent orchestration framework by Huginn Industries"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
